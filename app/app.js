angular.module('searcherApp', ['core'])

/*angular.module("searcherApp").config($routeProvider => {
    $routeProvider.when('/', {
        template: "<repo-list></repo-list>"
    }).when("/details",{
        template: "<repo-details><repo-details>"
    })
})*/
